const path = require("path");

// const result = path.join("folder1", "folder2", "subfolder", "file");// Combines the string
// console.log(result);

// const result = path.resolve("folder1", "folder2", "subfolder", "file");// Combines and give absolute path
// console.log(result);

// const result = path.extname("fs.js"); // Returns the extention of filename mentioned
// console.log(result);

// const result = path.basename(path.resolve("folder1", "folder2", "subfolder", "file.js")); // Returns the last portion of the path
// console.log(result);

// const result = path.parse(path.resolve("f3.txt")); // information about the file/directory in object format
// console.log(result.name);

